# Shuai

This repository contains a simple shell script for macOS that says "你好" every 10 seconds.

## Usage

Run `say_hello.sh` from the terminal:

```bash
./say_hello.sh
```

To stop the script, press `Ctrl+C`.

